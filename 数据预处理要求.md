任务需求：预处理OpenMathReasoning数据集
致 AI Agent:

您好！本次任务是创建一个Python脚本，用于对Hugging Face上的nvidia/OpenMathReasoning数据集进行预处理。

1. 任务目标

此脚本的核心目标是，将原始数据集中的问答对，转换为包含token ID和对应香农熵 (Shannon Entropy) 的结构化数据，并以JSON Lines (.jsonl) 格式保存。

这个预处理步骤旨在加速后续的量化校准流程，避免在每次运行时都重复进行高成本的熵计算。最终生成的openmathreasoning_prepared.jsonl文件将被我们的AQLM量化项目中的get_openmathreasoning_prepared函数直接读取。

2. 输入数据源

数据集: nvidia/OpenMathReasoning

数据分片 (Split): cot

关键字段:

problem (作为用户问题)

generated_solution (作为模型回答)

3. 核心处理逻辑

对于cot分片中的每一个样本，脚本需要执行以下操作：

加载模型与Tokenizer：

在脚本开始时，加载一个指定的基础大语言模型及其对应的Tokenizer。这将用于文本编码和熵值计算。

模型: meta-llama/Llama-2-7b-hf (或其他可配置的模型)。

Tokenizer: 与上述模型匹配的Tokenizer。

数据提取与格式化：

从每个样本中提取problem字段和generated_solution字段。

必须使用Tokenizer的聊天模板功能 (tokenizer.apply_chat_template) 将问题和答案格式化为一个包含用户和助手角色的对话序列。这确保了特殊token（如BOS, EOS, 角色标识符）被正确添加。

Token化与长度处理：

将格式化后的对话序列转换为input_ids。

定义一个目标序列长度，例如seq_len=4096（应可配置）。

长度检查:

如果生成的input_ids长度小于seq_len，则跳过该样本。

如果生成的input_ids长度大于或等于seq_len，则截断至seq_len。

熵计算 (使用vLLM)：

必须使用vllm库来高效地对所有待处理的文本进行批量前向传播，以获取每个token的logits。这对于处理大规模数据集至关重要。

对每个位置的logits计算其Softmax概率分布。

根据概率分布，计算该位置的香农熵。公式为：H(p)=−
sump_i
log_2p_i。

最终，为每个input_ids序列生成一个等长的、包含浮点数熵值的列表。

4. 输出文件规格

文件名: openmathreasoning_prepared.jsonl

文件格式: JSON Lines (.jsonl)。文件中的每一行都是一个独立的、有效的JSON对象。

编码: UTF-8

5. JSON对象结构

每一行的JSON对象必须包含以下两个键：

input_ids:

类型: List[int]

描述: Tokenizer生成的整数token ID列表，长度为seq_len。

entropies:

类型: List[float]

描述: 与input_ids一一对应的香农熵值列表。每个熵值都是一个浮点数。

6. 脚本的可配置性

生成的Python脚本应支持通过命令行参数进行配置：

--model_path: 指定用于计算熵的Hugging Face模型路径（默认为: meta-llama/Llama-2-7b-hf）。

--output_file: 指定输出的.jsonl文件路径（默认为: ./openmathreasoning_prepared.jsonl）。

--seq_len: 指定截断的目标序列长度（默认为: 4096）。

--num_samples (可选): 允许只处理数据集中的前N个样本，方便快速测试。如果未提供，则处理所有样本。

7. 错误处理与鲁棒性

脚本必须包含健壮的错误处理逻辑，检查各种边界条件：

数据校验: 在处理每个样本时，检查problem和generated_solution字段是否存在且不为空。如果缺失，应记录警告并跳过该样本。

长度一致性: 在保存到JSON对象之前，必须断言（assert）input_ids列表的长度与entropies列表的长度完全相等。如果不相等，应抛出致命错误并终止程序，因为这是数据处理流程中的严重bug。

依赖检查: 在脚本开始时，检查vllm, torch, transformers, datasets等必要的库是否已安装。如果未安装，应给出明确的提示信息并退出。

8. 输出示例

openmathreasoning_prepared.jsonl文件中的一行内容应如下所示：

{"input_ids": [1, 306, 465, 1073, 29901, 25, 13, 29892, 29958, 12, 23, 89, 2, ...], "entropies": [0.001234, 5.678901, 8.123456, 4.987654, 10.111213, 1.234567, 0.556677, 9.876543, 2.345678, 1.000123, 6.543210, 3.210987, 0.023456, ...]}

(注意: 上述ID和熵值为示例，非真实数据，且列表长度应为seq_len)

请根据以上需求，编写一个功能完整、代码健壮的Python脚本。